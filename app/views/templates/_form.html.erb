<%= form_for template do |form| %>
  <%= form.base_errors %>
  <%= form.text_field :name %>
  <%= form.text_field :question_limit %>
  <%= form.collection_check_boxes :topic_ids, Topic.all, :id, :name %>
  <%= form.submit %>
<% end %>
