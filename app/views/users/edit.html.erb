<section class="content-section">
  <div class="toolbar">
    <%= link_to t(:cancel), configure_path %>
  </div>
  <h1>
    <%= @user.name %> - <%= t('.title') %>
  </h1>
  <%= render 'form', user: @user, hide_password: true %>
</section>
